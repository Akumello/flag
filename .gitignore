# Node.js dependencies (anywhere in the repository)
**/node_modules/
node_modules/
**/npm-debug.log*
npm-debug.log*
**/yarn-debug.log*
yarn-debug.log*
**/yarn-error.log*
yarn-error.log*
**/lerna-debug.log*
lerna-debug.log*

# Package manager lock files (anywhere in the repository)
**/package-lock.json
package-lock.json
**/yarn.lock
yarn.lock
**/pnpm-lock.yaml
pnpm-lock.yaml

# Runtime data (anywhere in the repository)
**/pids
pids
**/*.pid
*.pid
**/*.seed
*.seed
**/*.pid.lock
*.pid.lock

# Coverage directory used by tools like istanbul (anywhere in the repository)
**/coverage/
coverage/
**/*.lcov
*.lcov

# nyc test coverage (anywhere in the repository)
**/.nyc_output
.nyc_output

# Grunt intermediate storage (anywhere in the repository)
**/.grunt
.grunt

# Bower dependency directory (anywhere in the repository)
**/bower_components
bower_components

# node-waf configuration (anywhere in the repository)
**/.lock-wscript
.lock-wscript

# Compiled binary addons (anywhere in the repository)
**/build/Release
build/Release

# Dependency directories (anywhere in the repository)
**/jspm_packages/
jspm_packages/

# TypeScript cache (anywhere in the repository)
**/*.tsbuildinfo
*.tsbuildinfo

# Optional npm cache directory (anywhere in the repository)
**/.npm
.npm

# Optional eslint cache (anywhere in the repository)
**/.eslintcache
.eslintcache

# Microbundle cache (anywhere in the repository)
**/.rpt2_cache/
.rpt2_cache/
**/.rts2_cache_cjs/
.rts2_cache_cjs/
**/.rts2_cache_es/
.rts2_cache_es/
**/.rts2_cache_umd/
.rts2_cache_umd/

# Optional REPL history (anywhere in the repository)
**/.node_repl_history
.node_repl_history

# Output of 'npm pack' (anywhere in the repository)
**/*.tgz
*.tgz

# Yarn Integrity file (anywhere in the repository)
**/.yarn-integrity
.yarn-integrity

# dotenv environment variables file (anywhere in the repository)
**/.env
.env
**/.env.test
.env.test
**/.env.production
.env.production
**/.env.local
.env.local
**/.env.development.local
.env.development.local
**/.env.test.local
.env.test.local
**/.env.production.local
.env.production.local

# parcel-bundler cache (anywhere in the repository)
**/.cache
.cache
**/.parcel-cache
.parcel-cache

# Next.js build output (anywhere in the repository)
**/.next
.next
**/.next/
.next/

# Nuxt.js build / generate output (anywhere in the repository)
**/.nuxt
.nuxt
**/dist
dist
**/dist/
dist/

# Gatsby files (anywhere in the repository)
**/.cache/
.cache/
**/public
public

# Vuepress build output (anywhere in the repository)
**/.vuepress/dist
.vuepress/dist

# Serverless directories (anywhere in the repository)
**/.serverless/
.serverless/

# FuseBox cache (anywhere in the repository)
**/.fusebox/
.fusebox/

# DynamoDB Local files (anywhere in the repository)
**/.dynamodb/
.dynamodb/

# TernJS port file (anywhere in the repository)
**/.tern-port
.tern-port

# Stores VSCode versions used for testing VSCode extensions (anywhere in the repository)
**/.vscode-test
.vscode-test

# VS Code settings (anywhere in the repository)
**/.vscode/
.vscode/
**/*.code-workspace
*.code-workspace

# JetBrains IDEs (anywhere in the repository)
**/.idea/
.idea/
**/*.iml
*.iml
**/*.iws
*.iws
**/*.ipr
*.ipr

# macOS (anywhere in the repository)
**/.DS_Store
.DS_Store
**/.AppleDouble
.AppleDouble
**/.LSOverride
.LSOverride
**/Thumbs.db
Thumbs.db

# Windows (anywhere in the repository)
**/Thumbs.db
Thumbs.db
**/ehthumbs.db
ehthumbs.db
**/Desktop.ini
Desktop.ini

# Linux (anywhere in the repository)
**/*~
*~

# Temporary files (anywhere in the repository)
**/*.tmp
*.tmp
**/*.temp
*.temp
**/*.log
*.log
**/*.swp
*.swp
**/*.swo
*.swo
**/*~
*~

# Build artifacts (anywhere in the repository)
**/dist/
dist/
**/build/
build/
**/out/
out/

# Google Apps Script (anywhere in the repository)
**/.clasp.json
.clasp.json
**/.clasprc.json
.clasprc.json

# Other common ignores (anywhere in the repository)
**/*.backup
*.backup
**/*.bak
*.bak
**/*.orig
*.orig
**/*.rej
*.rej